# CONTEXT — อภิธานศัพท์ของโปรเจกต์

คำศัพท์ที่ใช้ในโปรเจกต์นี้และความหมายที่ตกลงกันแล้ว ไฟล์นี้เป็น *อภิธานศัพท์เท่านั้น*
ไม่ใช่สเปก ไม่ใช่ที่จดวิธีทำ — รายละเอียดการ implement อยู่ใน CLAUDE.md และ ADR

## โครงสร้างเนื้อหา

**สื่อ (teaching medium)**
ไฟล์ HTML หนึ่งไฟล์ที่จบในตัวเอง เปิดด้วยดับเบิลคลิกได้ ตรงกับ "หนึ่งหัวข้อที่ครูฉายในคาบ"
ไม่ใช่ "หนึ่งหน้าเว็บ" — หน้า `index.html` ไม่ใช่สื่อ แต่เป็นสารบัญ

**แท็บ (tab)**
การแบ่งสื่อหนึ่งชิ้นออกเป็นหัวข้อย่อยที่ *เรียนแยกคาบได้* ผู้ใช้เห็นทีละแท็บ
ต่างจาก **การ์ด (card)** ซึ่งเป็นบล็อกเนื้อหาที่เรียงต่อกันภายในแท็บเดียว และเห็นพร้อมกันได้ด้วยการเลื่อนจอ

**สูตร (formula) / กฎ (rule)**
"สูตร" คือสิ่งที่อยู่ในตารางอ้างอิง (เช่น `∫ xⁿ dx = xⁿ⁺¹/(n+1) + C`) — เป็นผลลัพธ์ที่ท่องได้
"กฎ" คือสูตรที่บอกวิธี *ประกอบ* ฟังก์ชัน (ผลคูณ ผลหาร ลูกโซ่) — ในโค้ดใช้ class `.rule-card` ทั้งคู่ แต่ในคำอธิบายกับนักเรียนแยกกัน

**ตัวอย่างการคำนวณ (worked example)**
โจทย์ที่มีคำตอบและ *แสดงทุกขั้น* ที่คิดไว้ล่วงหน้าแล้ว (คงที่ ไม่สุ่ม) เขียนเป็นขั้น ① ② ③ ④
ต่างจาก **แบบฝึกหัด** ซึ่งไม่มีในโปรเจกต์นี้ (สื่อนี้ใช้ประกอบการสอน ไม่ใช่ให้ทำส่ง)

**พิสูจน์ (proof)**
การอนุมานที่แสดงว่า *ทำไม* ทฤษฎีบทถึงจริง ไม่ใช่ตัวอย่างการใช้ทฤษฎีบท —
เนื้อหาต่างชนิดกับตัวอย่างการคำนวณ จึงอยู่คนละแท็บกัน

**เครื่องมือเชิงโต้ตอบ (interactive tool)**
ต่างจาก **ตัวอย่างการคำนวณ** ตรงที่ผลลัพธ์ *คำนวณสดจากอินพุตที่นักเรียนพิมพ์เอง* ไม่คงที่ ไม่ได้เตรียมคำตอบไว้ล่วงหน้า
เช่น ตัวสร้างตารางค่าความจริงจากประพจน์ที่พิมพ์, ตัวพิสูจน์สมมูล/สัจนิรันดร์/สมเหตุสมผล (ดูหัวข้อตรรกศาสตร์ด้านล่าง) —
สองอย่างนี้อยู่คนละหมวดกันโดยเจตนา: ตัวอย่างการคำนวณคือของที่ครูโชว์ตายตัว ส่วนเครื่องมือเชิงโต้ตอบคือของที่นักเรียนป้อนเองแล้วเห็นผลของ *ของตัวเอง*

## ตรรกศาสตร์: คำที่ต้องแยกให้ชัด

**ประพจน์ (proposition)** vs **ประโยคเปิด (open sentence)**
ประพจน์ต้องมีค่าความจริงตายตัวโดยไม่ต้องกำหนดตัวแปรใดๆ (เช่น `3+2=5`) — ป้อนเข้าตัวสร้างตารางค่าความจริงได้
ประโยคเปิดมีตัวแปรอิสระที่ยังไม่ถูกผูกด้วยตัวบ่งปริมาณ (เช่น `x+2=3`) — ยังไม่มีค่าความจริงจนกว่าจะแทนค่าตัวแปรหรือใส่ ∀/∃ ครอบ
เครื่องมือเชิงโต้ตอบของแท็บตารางค่าความจริง/สมมูล/สัจนิรันดร์/สมเหตุสมผล รับเฉพาะ **ประพจน์** (ตัวแปร p,q,r ที่แจกแจงได้ครบด้วย T/F เท่านั้น สูงสุด 3 ตัว) — ไม่รับประโยคเปิดที่มี ∀/∃

**ทริก (trick)**
วิธีลัดเฉพาะของสรุปที่ครูจดไว้เอง (สมมติเป็นเท็จแล้วไล่ดูว่าขัดแย้งไหมสำหรับสัจนิรันดร์/สมเหตุสมผล, ใช้สมบัติ 23 ข้อแปลงข้างซ้ายขวาให้ตรงกันสำหรับสมมูล) — ไม่ใช่วิธีการทั่วไปในตำราทุกเล่ม แต่เป็นวิธีที่สื่อนี้ยึดเป็นหลักในการแสดงขั้นตอน เพราะเป็นวิธีที่ครูสอนนักเรียนจริง

**ตัวตัดสิน (arbiter)** vs **วิธีทำที่แสดง (displayed method)**
ใช้กับแท็บสมมูลโดยเฉพาะ: **ตัวตัดสิน** คือผลจากตารางค่าความจริง (แม่นยำ 100% เสมอ ตัดสินว่าสมมูลจริงหรือไม่) ส่วน **วิธีทำที่แสดง** คือลำดับการใช้สมบัติ 23 ข้อที่ค้นหามาแสดงเป็นทริก — วิธีทำอาจหาไม่เจอในบางเคสยาก (ต้องบอกตรงๆ ว่าหาไม่เจอ) แต่ตัวตัดสินต้องถูกเสมอ ห้ามให้วิธีทำที่หาไม่เจอไปเปลี่ยนคำตอบสุดท้าย

## สัญกรณ์คณิตศาสตร์

**เศษส่วนบน/ล่าง (stacked fraction)**
เศษส่วนที่ตัวเศษอยู่เหนือตัวส่วนคั่นด้วยเส้น — เป็นรูปแบบมาตรฐานของโปรเจกต์นี้
สแลชแนวนอน (`a/b`) ถือว่า *ไม่ได้มาตรฐาน* และให้แปลงทุกที่ที่เป็นเศษส่วนจริง

สิ่งที่ **ใช่** และต้องแปลง: `d/dx`, `dy/dx`, `0/0`, `∞/∞`, `π/2`, เลขชี้กำลังเศษส่วนอย่าง `x^(3/2)`

สิ่งที่ **ไม่ใช่** เศษส่วน แม้จะมีสแลชอยู่:
- **ตัวคั่นความหมาย "หรือ"** — `ผลบวก/ผลต่าง`, `จุดสูงสุด/ต่ำสุด`, `เล่น/หยุด`, `disk/washer`, `secⁿ/cscⁿ`
- **หน่วยวัด** — `ซม./วินาที`, `cm²/s`
- **ตัวนับลำดับ** — `ขั้นที่ 1 / 7`
- **ไวยากรณ์ที่ผู้ใช้ต้องพิมพ์เอง** — `1/x` ในตัวอย่างช่องพิมพ์สมการ ต้องคงเป็นสแลชเสมอ
  เพราะเป็นสิ่งที่ต้องพิมพ์ตามตัวอักษร ไม่ใช่สิ่งที่ต้องอ่านเป็นคณิตศาสตร์
- **พาธไฟล์** — `docs/adr/0001`, `derivatives/index.html`

**สื่อ vs สารบัญ:** กฎเศษส่วนบน/ล่างใช้กับ *สื่อ* เท่านั้น หน้า `index.html` เป็นสารบัญ
ข้อความบนการ์ดที่นั่นเป็นคำโปรยเพื่อค้นหา ไม่ใช่คณิตศาสตร์ที่นักเรียนต้องอ่านตาม
`(0/0, ∞/∞)` ในคำโปรยจึงคงเป็นสแลช

## แคลคูลัส: คำที่เคยปนกัน

**ปฏิยานุพันธ์ (antiderivative)** — ฟังก์ชัน `F` ที่ `F′ = f` *ตัวใดตัวหนึ่ง*
**ปริพันธ์ไม่จำกัดเขต (indefinite integral)** — `∫f(x)dx = F(x) + C` คือ *ตระกูลทั้งหมด* ของปฏิยานุพันธ์
สองคำนี้ไม่ใช่คำเดียวกัน: ปฏิยานุพันธ์คือสมาชิกหนึ่งตัว ปริพันธ์ไม่จำกัดเขตคือทั้งเซต

**ปริพันธ์จำกัดเขต (definite integral)** — `∫ₐᵇ f(x)dx` เป็น *จำนวน* ไม่ใช่ฟังก์ชัน

**A(x) — ฟังก์ชันพื้นที่สะสม**
พื้นที่ใต้ `y=f(x)` จาก `a` ถึง `x` มีเงื่อนไขผูกติดมาว่า `A(a) = 0` เสมอ
**F(x) — ปฏิยานุพันธ์ใดๆ ของ f** ไม่มีเงื่อนไขที่ `a`

การแยกสองตัวนี้คือหัวใจของพิสูจน์ทฤษฎีบทหลักมูลภาค 2: `F(x) = A(x) + C` และเพราะ `A(a)=0`
จึงได้ `F(a) = C` ซึ่งทำให้ `C` ตัดกันตอน `F(b) − F(a)` — ถ้าเรียก A กับ F ปนกันเป็นตัวเดียว พิสูจน์จะพัง

**ทฤษฎีบทหลักมูล ภาค 1** — "ดิฟตัดอินทิเกรต": `d/dx ∫ₐˣ f(t)dt = f(x)`
**ทฤษฎีบทหลักมูล ภาค 2** — "สูตรคำนวณ": `∫ₐᵇ f(x)dx = F(b) − F(a)`
ในตำราไทยบางเล่มพิสูจน์ภาค 2 โดยฝังข้อโต้แย้งของภาค 1 ไว้ข้างใน — ในสื่อนี้แยกสองภาคเป็นคนละพิสูจน์อย่างชัดเจน
